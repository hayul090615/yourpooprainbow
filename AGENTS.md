# 장기 개발 규칙

## 프로젝트 단계와 역할

- 이 프로젝트는 2명이 4주 동안 공동 개발한다.
- 현재는 프론트엔드 개발 단계다.
- 프론트엔드 담당자는 React UI와 프론트엔드 기능을 개발한다.
- 팀원은 Node.js, Express, TypeScript 백엔드와 PostgreSQL 데이터베이스를 담당한다.
- 백엔드와 PostgreSQL은 팀원이 별도로 개발하며, 요청 없이 구현하거나 대규모 수정하지 않는다.
- 실제 프론트엔드·백엔드 연결은 백엔드 API가 준비된 뒤 진행한다.
- API 연결이 필요해지면 먼저 필요한 요청·응답 형식을 설명한다.

## 기술 스택

- Frontend: React, Vite, TypeScript
- Backend: Node.js, Express, TypeScript (팀원 담당)
- Database: PostgreSQL (팀원 담당)

현재 프론트엔드는 React + Vite + TypeScript로 전환되어 있으며, 진입점과 주요 코드는 `src` 아래에 있다. 루트에 남아 있는 기존 파일이나 중복 구조는 요청 없이 삭제하거나 재구성하지 않는다.

`backend` 디렉터리에는 팀원이 개발한 Express + TypeScript + PostgreSQL 코드가 있다. 프론트엔드 작업 중에는 참고만 하고, 명시적인 요청 없이 수정하지 않는다.

## 프론트엔드 구조

현재 React 프론트엔드는 다음 구조를 사용한다.

- `src/components`: 재사용 가능한 UI 컴포넌트
- `src/pages`: 페이지 단위 컴포넌트
- `src/services`: API 호출과 임시 데이터 접근 영역
- `src/types`: TypeScript 타입
- `src/assets`: 이미지와 정적 파일

- 재사용 가능한 컴포넌트를 우선하고, 한 컴포넌트에 너무 많은 기능을 넣지 않는다.
- UI 코드와 데이터 처리 로직을 가능한 한 분리한다.
- 데이터 구조와 타입을 명확히 작성해 실제 API로 쉽게 교체할 수 있게 한다.

## 데이터와 API

- 백엔드가 완성되기 전에는 mock 데이터 또는 임시 데이터만 사용한다.
- 프론트엔드에서 PostgreSQL에 직접 접근하거나 연결 코드를 작성하지 않는다.
- 백엔드 API가 없다는 이유로 백엔드 코드를 생성하지 않는다.
- 나중에 API로 교체할 코드는 `src/services` 등 별도 영역에 분리한다.

## Git 협업 규칙

- 작업 전에 현재 Git 상태와 관련 파일을 확인한다.
- 최신 `main`을 기준으로 기능 브랜치에서 작업하고 Pull Request로 병합한다.
- 작업 단위마다 의미 있는 커밋을 만든다. 커밋 메시지는 변경 내용을 분명히 설명한다.
- 다른 사람의 변경을 덮어쓰거나 `main`의 기존 코드를 임의로 삭제·초기화하지 않는다.
- 충돌이 생기면 임의로 삭제하거나 해결하지 말고 충돌 내용을 먼저 설명한다.

## Codex 작업 규칙

- 수정 전 현재 프로젝트 구조와 관련 파일을 먼저 확인한다.
- 기존 코드를 확인한 후 필요한 부분만 수정하며, 기존 기능을 임의로 삭제하지 않는다.
- 요청하지 않은 기능·라이브러리를 추가하지 않는다. 새 라이브러리가 필요하면 설치 전에 알린다.
- 파일 삭제 전에는 반드시 확인을 받는다.
- 프론트엔드 작업 후 `npm run build`로 TypeScript 오류와 Vite 빌드 오류를 확인한다.
- 백엔드를 요청받아 수정한 경우에만 `npm --prefix backend run typecheck`와 필요한 백엔드 검증을 수행한다.
- `.env`와 `.env.local`의 비밀값을 출력하거나 커밋하지 않는다. 새 환경변수가 필요하면 예시 값만 `.env.example`에 반영한다.
- 완료 시 수정한 파일과 변경 내용을 짧게 요약한다.
