import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yourpooprainbow.toiletmap',
  appName: '니똥칼라똥',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
    url: 'https://yourpooprainbow.vercel.app',
  },
};

export default config;
