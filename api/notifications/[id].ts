import { pool } from '../../backend/src/db/pool.js';
import { findSessionUser } from '../../backend/src/services/auth-service.js';
type ApiRequest = { method?: string; query: { id?: string | string[] }; headers: { authorization?: string | string[] } };
type ApiResponse = { status(code: number): ApiResponse; json(body: unknown): void; end(): void };
export default async function handler(request: ApiRequest, response: ApiResponse) { if (request.method !== 'PATCH') { response.status(405).json({ message: 'PATCH 요청만 지원합니다.' }); return; } const auth = Array.isArray(request.headers.authorization) ? request.headers.authorization[0] : request.headers.authorization; const user = await findSessionUser(auth); if (!user) { response.status(401).json({ message: '로그인이 필요합니다.' }); return; } const id = Array.isArray(request.query.id) ? request.query.id[0] ?? '' : request.query.id ?? ''; if (!/^[1-9]\d*$/.test(id)) { response.status(400).json({ message: '알림 번호가 올바르지 않습니다.' }); return; } try { await pool.query('UPDATE public.user_notifications SET read_at = CURRENT_TIMESTAMP WHERE id = $1 AND recipient_user_id = $2;', [id, user.id]); response.status(204).end(); } catch (error) { console.error('Failed to read notification:', error); response.status(500).json({ message: '알림을 처리하지 못했습니다.' }); } }
